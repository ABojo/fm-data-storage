{
  "name": "design-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-server": "cd dist && live-server",
    "build-sass": "sass --watch --no-source-map src/sass/main.scss dist/css/index.css",
    "dev": "npm-run-all --parallel start-server build-sass"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.60.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
